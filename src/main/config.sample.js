/**
 * Application configuration
 */
export class Configuration {
  /**
   * Webapp will be loaded from the following URL.
   * @type {string} URL
   */
  static WEBAPP_URL = "<URL FOR THE WEBAPP>";
}
